# 專案更新紀錄 (2026-04-06)

## 變更摘要
1. **排解 Bad credentials 錯誤**：確認為雲端部署 (Render) 端的 `GITHUB_PAT` 失效，指引使用者前往 Render Dashboard 更新。
2. **新增手冊**：根據 `?room=` 參數特性，撰寫了《教學分組與 URL 使用手冊》，放置於 `docs/url-routing-manual.md`。
3. **首頁 README 更新**：將預設的 Vite 範本移除，換上 Markdown-Collab 的簡介與文件連結。
4. **開發紀錄歸檔**：將本對話的 `task.md` 與 `walkthrough.md` 同步備份至專案的 `docs/` 目錄。
